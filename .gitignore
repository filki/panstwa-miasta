# Build outputs (frontend)
frontend-mobile/www/
frontend-desktop/dist/

# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Node modules
node_modules/

# Coverage and Sonar
coverage/
coverage-js/
coverage.xml
.coverage
test-results.xml
.sonar/
.scannerwork/

# Test / scanner output dumps (local-only)
test_output.txt
jest_full_output.txt
*.output.txt

# Lokalne zrzuty PKD / surowe listy (gra: SQLite + *_seed.py) — w backendzie
backend/data/

# GeoNames źródło (pobierz: https://download.geonames.org/export/dump/cities15000.zip)
backend/scripts/cities15000.txt

# Wyjście backend/scripts/extract_jobs.py
backend/scripts/pkd_raw_jobs.txt

# Ruff cache
.ruff_cache/

# Logs and DB
logs/
*.db

# Local secrets (e.g. NGROK_AUTHTOKEN for docker-compose)
.env

# PESEL CSV extracts (seed lives in names_seed.py)
**/8_-_Wykaz_imion_męskich*.csv
**/8_-_Wykaz_imion_żeńskich*.csv

# MCP memory graph — dynamic, agent-managed, machine-local
.zed/memory.json

# Cursor config (migrated to .zed/)
.cursor/

# Zed local config (machine-specific paths)
.zed/

# Google Cloud credentials
projekt-testowy-*.json
*.db-wal
*.db-shm
*.db?mode=ro
.codegraph/
