{
  "name": "panstwa-miasta",
  "version": "1.0.0",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "jest --coverage",
    "css:dev": "npx @tailwindcss/cli -i static/css/theme.css -o static/css/dist.css --watch",
    "css:build": "npx @tailwindcss/cli -i static/css/theme.css -o static/css/dist.css --minify",
    "cap:sync": "npx cap sync",
    "cap:android": "npx cap sync android && npx cap open android",
    "cap:build": "npx cap sync android"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "collectCoverage": true,
    "coverageDirectory": "coverage-js",
    "coverageReporters": [
      "text",
      "lcov"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "jest": "^30.4.2",
    "jest-environment-jsdom": "^30.4.1",
    "tailwindcss": "^4.3.0"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.4",
    "@capacitor/cli": "^8.3.3",
    "@capacitor/core": "^8.3.3",
    "@capacitor/share": "^8.0.1",
    "n8n-nodes-langchain": "^1.2.0"
  }
}
